package dao;

import java.sql.SQLException;
import java.util.Date;

import modelo.Evento;

public class EventoDAO {
	
	public boolean crearEvento(Evento evento) {
		
		boolean creado = false;
		
		/*String deporte = evento.getDeporte();
		String Localizacion = evento.getLocalidad();
		String tiempo = evento.getHoraEvento();
		String comentarios = evento.getComentarios();
		String genero = evento.getRequisitos().getRequisitoDeGenero();
		
		Date fechaEvento = evento.getFechaEvento();
		Date fechaCreacion = new Date(System.currentTimeMillis());
		
		float reputacionNecesaria = evento.getRequisitos().getReputacionNecesaria();
		float costeEvento = evento.getCosteEvento();
		
		int maxParticipantes = evento.getMaximoParticipantes();
		int edadMaxima = evento.getRequisitos().getEdadMaxima();
		int edadMinima = evento.getRequisitos().getEdadMinima();
		
		try {
			Conexion conexion = new Conexion();
			
			//comprobar y crear
			
		} catch (ClassNotFoundException | SQLException e) {
			e.printStackTrace();
		}*/
		
		return creado;
		
	}
}
